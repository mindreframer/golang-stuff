package dynamodb_test

import (
	"testing"
)

func setup(t *testing.T) bool {
	/*
		c, err := dynamodb.Connect()

		if err != nil {
			t.Logf("Skipping test: could not connect to DynamoDB Service : %s", err)
			return false
		}

		c.Close()

		return true
	*/
	return true
}
